 Global context is created to give the entire app access to resources such as state and props
 It is created with the ``createContext`` hook and consumed with the ``useContext`` hook.
 state and props can be gotten by destructuring them out of the context function that points to the
context that has been provided. See ``context.jsx`` file and how it is consiumes in the ``app.jsx`` file

