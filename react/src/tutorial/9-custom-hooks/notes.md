Using custom hook like the one here in this folder is useful when you have different components that have the same data fetching functionality and you don't what to repeat yourself everytime.

- You can make custom hooks for toggles
- you can use custom hooks to create hooks for data fetching

## Rules of custom Hooks

- All custom hooks should start with the word "use"
- A custom hook must use at least one built in react hook
