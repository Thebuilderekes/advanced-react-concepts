Using custom hook like the one here in this folder is useful when you have different components that have the same data fetching functionality.

- You can make custom hooks for toggles
- data fetching

-All custom hooks should start with the word "use"
