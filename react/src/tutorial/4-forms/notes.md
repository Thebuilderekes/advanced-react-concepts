## Onchange and Value - Very important for form inputs

The `onChange ={handleChange}` function is what you use to track changes on a form input
value should be set to the name of the state value you are expecting from that input e.g
`value={name}`
