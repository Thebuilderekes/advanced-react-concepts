## Split Screen Layout

Using this pattern you can create a layout as in the `splitScreenLayout.jsx` and have that layout to be reusuable throughout your app while you create the exact content that you want in it as in the `HomeScreen.jsx`
